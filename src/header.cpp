#include <iostream>
#include <string>
#include <math.h>
#include "header.h"

int  h1(int value, int n){
  lastbits = pow(2,n) - 1
  return value & lastbits;
}

int ** histogram(tuple *R){
  int size = sizeof(R)/sizeof(*R);
  int * values;
  for(int i = 0; i < size; i++){

  }
}
